# Placeholder for CMake Diff, if you're dead set on trying this.
target_include_directories(Luau.VM PUBLIC VM/include) -> target_include_directories(Luau.VM PUBLIC VM/include Compiler/include)
target_link_libraries(Luau.VM PUBLIC Luau.Common) - > target_link_libraries(Luau.VM PUBLIC Luau.Common Luau.Compiler)

# Removed.
message(FATAL_ERROR ${LIB} " is a runtime component but it depends on one of the offline components")
